# 学术同行评议平台 · **问题二：申报材料语义理解与要点生成** · 接口文档

> 版本：v1.0 · 日期：2025-10-31 · 状态：草案（可对接开发）  
> 模块来源：与原型中“问题二：申报材料语义理解与要点生成”界面一一对应（多维度评价展示、回避与分派建议、能力结构分析、代表性成果速览、材料内在逻辑主线、AI 结构化评审摘要、导出）。

---

## 1. 总览

- **模块目标**：对申报材料（项目书、论文摘要、教学总结、获奖与社会服务佐证等）进行多源语义解析、要点抽取、结构化摘要生成，并输出评审辅助信息与可下载的评审摘要。
- **API 风格**：REST + JSON；长耗时任务采用「任务轮询」或 Webhook 回调。
- **Base URL（示例）**：`https://api.peerreview.example.com/v1`
- **认证**：`Authorization: Bearer <token>`；服务间可采用 OAuth2 Client Credentials。
- **Header 约定**：
  - `X-Request-Id`：可选，用于链路追踪。
  - `Idempotency-Key`：**建议**对所有 `POST` 写操作提供，保证幂等。
  - `Accept-Language`：`zh-CN` / `en-US`，影响自然语言摘要语言。
- **数据保密**：上传材料建议先脱敏；平台需符合院校/地方法规（存储区域、访问审计、数据保留期限可配置）。
- **速率限制**：默认 120 rpm / 租户，可协商。

---

## 2. 关键对象模型

### 2.1 Material（申报材料）
```json
{
  "id": "mat_123",
  "name": "教学改革总结.pdf",
  "type": "pdf|docx|txt|image",
  "size_bytes": 2345678,
  "hash_sha256": "…",
  "meta": {
    "applicant_name": "王培军",
    "department": "信息工程学院",
    "position_applied": "教授（科研岗）"
  },
  "created_at": "2025-09-12T03:21:45Z"
}
```

### 2.2 AnalysisResult（语义解析结果，核心聚合体）
```json
{
  "id": "ars_789",
  "materials": ["mat_123", "mat_456"],
  "weights": {
    "research": 0.62,
    "teaching": 0.28,
    "service": 0.10,
    "topics": ["跨模态学习行为证据链","教育场景知识图谱","可信推荐模型"]
  },
  "key_points": {
    "organization": "南京大学",
    "disciplines": ["计算机科学与技术","人工智能"],
    "keywords": ["知识图谱","推荐系统","教育数据治理"]
  },
  "logic_axes": {
    "themes": ["智能推荐可信与可解释","教育场景知识图谱","多源学习行为融合"],
    "methods": ["跨模态语义对齐","证据链追踪","数据驱动评估"],
    "evidence": ["ESI高被引论文6篇","省部级奖励4项","重大项目2项"]
  },
  "capability_profile": {
    "originality": 86,
    "engineering": 82,
    "talent_training": 78,
    "organization": 80,
    "rationales": {
      "originality": "提出“跨模态学习行为证据链”等方法并被持续引用",
      "engineering": "“学习行为可信分析平台”落地6所高校"
    }
  },
  "representative_outputs": {
    "papers_5y": 26,
    "ccf_a": 8,
    "awards": ["省部级二等奖（第一完成人）"],
    "projects": ["国家重点研发计划子课题1项","教育部/省科技厅重大专项各1项"],
    "teaching_highlights": ["博士生省优博2名","课堂数据→反馈→课程迭代闭环"]
  },
  "suggestions": {
    "avoidance": [
      "回避南京大学及其附属单位专家（同单位）",
      "近五年合作（论文/专利/项目）需检索并回避",
      "导师-学生/同团队从属关系核查并回避"
    ],
    "assignment": [
      "优先分配到“计算机科学与技术/人工智能”方向评审专家",
      "结合地域与职称结构优化评审席位"
    ],
    "notes": "算法仅给出建议，需人工最终确认"
  },
  "summary_sections": [
    {"title":"学术背景与岗位适配","content":"长期从事智能推荐与教育数据治理研究，可独立主持国家级项目；与科研岗匹配度高。"},
    {"title":"科研创新与影响力","content":"提出可解释证据链方法，产出高被引论文并获多项奖励。"},
    {"title":"代表性成果（建议重点看）","content":"授权发明专利、重大项目、平台落地与教学成效。"}
  ],
  "created_at": "2025-10-01T09:30:00Z"
}
```

---

## 3. 接口一览

| 方法 | 路径 | 说明 |
|---|---|---|
| `POST` | `/materials` | 上传申报材料（支持批量） |
| `POST` | `/analysis/tasks` | 发起语义解析任务 |
| `GET` | `/analysis/tasks/{task_id}` | 查询任务状态（`queued|running|succeeded|failed`）|
| `GET` | `/analysis/results/{result_id}` | 获取聚合的语义解析结果 |
| `POST` | `/exports` | 发起导出（`pdf|docx|md`） |
| `GET` | `/exports/{export_id}/download` | 下载导出产物 |
| `GET` | `/taxonomy/disciplines` | 获取学科枚举/别名表 |
| `GET` | `/version` | 服务版本与健康检查 |

---

## 4. 端点详解

### 4.1 上传申报材料 `POST /materials`
- **Content-Type**：`multipart/form-data`
- **表单字段**：
  - `files[]`: 任意多个文件（pdf/docx/txt/image）
  - `meta`（可选，JSON 字符串）：`applicant_name`, `department`, `position_applied` 等
- **返回** `201 Created`
```json
{
  "materials": [
    {"id":"mat_123","name":"教学改革总结.pdf"},
    {"id":"mat_456","name":"科研项目书.docx"}
  ]
}
```
- **错误**：`413 Payload Too Large`、`415 Unsupported Media Type`、`422 Unprocessable Entity`

### 4.2 发起语义解析 `POST /analysis/tasks`
- **请求体**
```json
{
  "materials": ["mat_123","mat_456"],
  "options": {
    "language": "zh-CN",
    "enable_sections": ["weights","key_points","logic_axes","capability_profile","representative_outputs","summary_sections","suggestions"],
    "dedup_strategy": "semantic",
    "llm_model": "auto"
  },
  "webhook_url": "https://example.com/hooks/analysis",
  "idempotency_key": "b6a6e1a1-..."
}
```
- **返回** `202 Accepted`
```json
{"task_id":"tsk_001","status":"queued","eta_seconds":120}
```

### 4.3 查询任务 `GET /analysis/tasks/{task_id}`
```json
{"task_id":"tsk_001","status":"succeeded","result_id":"ars_789","progress":100}
```

### 4.4 获取结果 `GET /analysis/results/{result_id}`
- **查询参数**：`fields`（可选，逗号分隔以裁剪字段，如 `weights,key_points,summary_sections`）
- **返回**：见 2.2 `AnalysisResult`

### 4.5 导出评审摘要 `POST /exports`
- **请求体**
```json
{
  "result_id":"ars_789",
  "format":"pdf",
  "template":"default", 
  "watermark": "INTERNAL",
  "filename": "评审摘要_王培军.pdf"
}
```
- **返回**
```json
{"export_id":"exp_001","status":"queued"}
```

### 4.6 下载导出 `GET /exports/{export_id}/download`
- **返回**：`302` 重定向到一次性下载 URL；或直接 `200` 二进制流（取决于部署）

### 4.7 学科枚举 `GET /taxonomy/disciplines`
```json
{"disciplines":["计算机科学与技术","人工智能","数据挖掘","自然语言处理", "..."]}
```

### 4.8 健康检查 `GET /version`
```json
{"service":"peerreview-material-nlp","version":"1.0.0","commit":"abc123","uptime_seconds":123456}
```

---

## 5. 错误码约定

| HTTP | 错误码 | 说明 |
|---|---|---|
| 400 | `bad_request` | 参数不合法 |
| 401 | `unauthorized` | 缺少或错误的令牌 |
| 403 | `forbidden` | 无访问权限 |
| 404 | `not_found` | 资源不存在 |
| 409 | `conflict` | 幂等冲突/资源版本冲突 |
| 413 | `payload_too_large` | 文件过大 |
| 415 | `unsupported_media_type` | 不支持的文件类型 |
| 422 | `unprocessable_entity` | 无法解析（文件损坏/空白） |
| 429 | `rate_limited` | 触发限流 |
| 500 | `internal` | 服务异常 |
| 503 | `unavailable` | 后端容量不足/降级 |

**错误响应体**
```json
{
  "error": {
    "code": "unprocessable_entity",
    "message": "OCR 失败：图片分辨率过低",
    "request_id": "req_xxx",
    "hint": "请上传 300DPI 以上扫描或原始电子文档"
  }
}
```

---

## 6. Webhook（可选）

- 事件：`analysis.completed`
```json
{
  "event":"analysis.completed",
  "task_id":"tsk_001",
  "result_id":"ars_789",
  "tenant_id":"tn_001",
  "ts":"2025-10-31T09:00:00Z"
}
```

**签名校验**：`X-Signature: sha256=…`（内容为原始请求体 HMAC-SHA256）

---

## 7. 字段与原型界面映射（关键块）

| 原型界面元素 | 接口字段 |
|---|---|
| 多维度评价展示（科研/教学/社会服务权重与主题） | `AnalysisResult.weights` |
| 申报材料要点（单位、学科、关键词） | `AnalysisResult.key_points` |
| 回避与分派建议 | `AnalysisResult.suggestions` |
| 代表性成果速览 | `AnalysisResult.representative_outputs` |
| 材料内在逻辑主线（三栏主题/方法/支撑） | `AnalysisResult.logic_axes` |
| 能力结构分析（四维能力） | `AnalysisResult.capability_profile` |
| AI 结构化评审摘要 | `AnalysisResult.summary_sections` |
| 导出评审摘要按钮 | `POST /exports` + `GET /exports/{id}/download` |

---

## 8. 安全与合规建议（实现指引）

1. **数据分区与加密**：租户级命名空间；存储与传输全链路加密（KMS 管理密钥）。
2. **可审计**：记录上传、访问、导出操作日志（含 `X-Request-Id`）。
3. **最小化存储**：材料解析后可保留文本向量与结构化结果，原文可选择 T+N 天自动清理。
4. **模型可追踪**：在结果对象中记录 `model`, `prompt_hash`, `dataset_version` 以便复现。
5. **人工在环**：对“回避/分派建议”明确标注“仅建议，需人工确认”。

---

## 9. 示例：一键跑通（伪代码）

```bash
# 1) 上传
curl -H "Authorization: Bearer $TOKEN" -F "files[]=@教学改革总结.pdf" \
     -F 'meta={"applicant_name":"王培军","department":"信息工程学院"}' \
     https://api.peerreview.example.com/v1/materials

# 2) 发起解析
curl -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
     -d '{"materials":["mat_123"],"options":{"language":"zh-CN"}}' \
     https://api.peerreview.example.com/v1/analysis/tasks

# 3) 轮询状态
curl -H "Authorization: Bearer $TOKEN" \
     https://api.peerreview.example.com/v1/analysis/tasks/tsk_001

# 4) 取结果
curl -H "Authorization: Bearer $TOKEN" \
     "https://api.peerreview.example.com/v1/analysis/results/ars_789?fields=weights,key_points,summary_sections"

# 5) 导出
curl -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
     -d '{"result_id":"ars_789","format":"pdf","filename":"评审摘要_王培军.pdf"}' \
     https://api.peerreview.example.com/v1/exports
```

---

## 10. 版本规划（Roadmap）

- v1.1：支持 **自定义导出模板**（Markdown 模板 + 变量）、**多语摘要**、**学科词表在线维护**。
- v1.2：新增 **冲突检测 API**（从外部论文/基金库检索近五年合作关系）。

—— 以上为问题二模块的接口契约，可直接交由后端实现并与前端联调。
