[project]
name = "peer-material-service"
version = "0.1.0"
description = "Academic peer review material processing service"
authors = ["AutoGenerated <auto@example.com>"]
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest",
]
llm = [
    "langgraph>=0.1.1",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
